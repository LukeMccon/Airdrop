# Fixes Checklist

## Completed

- [x] 1. Fix falling speed config path mismatch (`drop.falling-speed` vs `drop.parachute.falling-speed`).
- [x] 2. Prevent crate removal on canceled `BlockBreakEvent` in `CrateDestroyListener`.
- [x] 3. Charge economy before spawning the crate (or roll back drop on charge failure).
- [x] 4. Fix mutable `Location` drift in `ParachuteSystem` chicken/slime spawn logic.
- [x] 5. Handle package inventory overflow (GUI allows more items than barrel capacity).
- [x] 6. Decide and implement intended behavior for landed barrels on chunk unload cleanup.
- [x] 7. Only delete barrels on close if they are tracked crates (`CrateCloseListener` currently removes any empty barrel).
- [x] 8. Validate package prices (non-negative, finite) to prevent economy exploits with negative/invalid values.
- [x] 9. Ensure `Crate.destroy()` also cleans up the falling barrel entity (or restores gravity) to avoid stuck ghost crates.
- [x] 10. Make `/airdrop reload` reload `packages.yml`/GUI state, not just `config.yml` and language.
- [x] 11. Respect cancelled cleanup events (`BlockBurnEvent`, `BlockExplodeEvent`, `EntityExplodeEvent`) to avoid crate-state desync.
- [x] 17. Make admin checks sender-agnostic: `PermissionsHelper.isAdmin(CommandSender)` now handles RCON/other sender types.
- [x] 18. Normalize package permission node generation with `Locale.ROOT` and align casing rules.
- [x] 19. Declare permission nodes and inheritance in generated `plugin.yml`.
- [x] 20. Remove falling block from `CrateManager.crateMap` when crate lands. `FallingCrateListener:29` now uses `CrateManager.removeCrate(fallingBlock)` to atomically remove and retrieve the crate entry.
- [x] 21. Fix location mismatch between storage and lookup in landed crate management. `CrateManager` now uses a `BlockKey` record (world UUID + block x/y/z) eliminating world reference equality issues.
- [x] 25. Fix landed location mutation from landing effects. `RenderPackageLandedTask` now clones incoming `Location` before offsetting and `Crate.land()` passes a clone, preserving `Crate.landedLocation`.
- [x] 36. Fix race condition between `hasCrate()` and `getCrate()` in `FallingCrateListener`. Now uses single atomic `removeCrate()` call.
- [x] 38. Run landed/glow/smoke particle tasks on the main thread. `Crate.land()` now uses sync scheduler variants (`runTask` / `runTaskTimer`) for Bukkit particle calls.
- [x] 40. Fix null-check for crate lookup in `FallingCrateListener`. `removeCrate(fallingBlock)` result is null-checked at line 30.
- [x] 71. Fix stale null-world entries in `landedCrateMap`. `CrateManager` now uses `BlockKey` record which stores world UUID, so null world references in `Location` objects are handled at key creation time.

## Open - Critical

- [ ] 39. **CRITICAL**: `Package.getItems()` (`Package.java:171-172`) returns the internal `items` list reference directly. Callers can mutate the package's items without going through `setItems()`. This compounds issue #12 — `DropController.dropPackageAtLocation():149` passes `pkg.getItems()` to `Crate` constructor, and both the `Package` and `Crate` end up sharing the same list reference. Any mutation through either reference corrupts the other. `Package.setItems()` (`Package.java:175-177`) also stores the provided list reference directly, so external callers can keep mutating package state after assignment. Fix: make defensive copies in `getItems()` and `setItems()`. **Audit 2026-02-08: confirmed real in current code.**
- [ ] 57. **CRITICAL**: `ConfigKeys.getConfig():71-72` calls `Airdrop.getConfiguration().getConfig()` without null-checking. After `onDisable()` sets `configuration = null` (Airdrop.java:110), any later call to a `ConfigKeys` getter throws `NullPointerException`. Startup ordering in current code initializes `configuration` before first `ConfigKeys` use, but the helper remains null-unsafe for shutdown/race paths and any late/static access. **Audit 2026-02-08: confirmed real null-safety issue in current code.**
- [ ] 58. **CRITICAL**: No refund on crate spawn failure after player is charged. `DropController.playerInitiatedDropPackage():132-134` calls `pkg.chargeUser(player)` then `dropPackageAtLocation()`. If `dropPackageAtLocation()` throws any RuntimeException (entity spawn failure, world unloaded, etc.), the player has been charged but receives no package and no refund. There is no try/catch around the drop to refund on failure. **Audit 2026-02-08: confirmed real in current code.**
- [ ] 103. **CRITICAL**: `CrateManager.removeCrate(FallingBlock):36-37` returns the crate but does NOT call `crate.destroy()`, and `FallingCrateListener:29` uses this overload before calling `landedCrate.land(...)`. Not calling `destroy()` is intentional for successful landing, but there is no failure rollback: if `land()` throws, the crate has already been removed from `crateMap` and cleanup is skipped, leaving parachute/task resources orphaned. (Currently this overload is only called from landing flow.) **Audit 2026-02-08: confirmed real failure-path bug in current code.**

## Open - High

- [ ] 12. Deep-clone package `ItemStack`s per drop so inventory insertion cannot mutate template items across future drops. `Crate` constructor copies the list (`new ArrayList<>(contents)`) but the `ItemStack` references inside are still shared with `Package.items`. When `barrel.getInventory().addItem(is)` is called in `Crate.land():115-116`, Bukkit may mutate the stack (e.g., splitting stacks), affecting the template package for all subsequent drops. **Audit 2026-02-08: confirmed real in current code.**
- [ ] 13. Fix package name case-handling mismatch in GUI selection (`PackagesGui` lowercases clicked display name before lookup, which breaks mixed-case package names from config). Root cause: `PackagesGui.onInventoryClick():94` lowercases the display name before package lookup, but `PackageManager.get()` uses exact string matching on the `HashMap`. If a package is named "StarterPack" in config, the lookup with "starterpack" will fail. **Audit 2026-02-08: confirmed real in current code.**
- [ ] 14. Stop mutating crate state via location side effects in `RenderPackageLandedTask` constructor. Line 27 calls `location.add(reposition)` which mutates the caller's `Location` object (the crate's `landedLocation` field). This shifts the crate's stored location by (0.5, 0.5, 0.5) permanently. (Duplicate of #25 — fix one to fix both.) **Audit 2026-02-08: confirmed real duplicate of #25.**
- [ ] 15. Guard barrel holder casts in inventory listeners (`CrateOpenListener:22`, `CrateCloseListener:22`) with `instanceof` check before casting. `e.getInventory().getHolder()` may not be a `Barrel` even when inventory type is `BARREL` (e.g., custom InventoryHolder implementations), causing `ClassCastException`.
- [ ] 16. Validate numeric drop settings (`falling-speed`, `drop.height`, chicken count, smoke height) to reject invalid/unsafe values (e.g., negative speed or height causing broken drop behavior). A very large chicken count (e.g., 10000) would spawn thousands of entities and crash the server. Negative speed makes the crate fall upward. Speed of 0 makes it float forever.
- [ ] 22. Add defensive null check for `loc.getWorld()` in `FallingCrateListener.onEntityChangeBlockEvent():35` before using `world`. The `Location.getWorld()` can return null if the world has been unloaded.
- [ ] 23. Avoid blocking main thread in `PermissionsHelper.initialize():98,102,106,110` — the `.join()` calls on LuckPerms async `CompletableFuture` operations block the server's main thread during startup. If LuckPerms is slow or unresponsive (e.g., remote database), the entire server hangs. Four sequential `.join()` calls compound the risk.
- [ ] 24. Add null safety checks for `Airdrop.getPluginInstance()` calls in async tasks (`Crate.java:84,124,129,134`) to handle edge cases where plugin is disabled during active crate operations.
- [ ] 26. **HIGH**: `ParachuteSystem.parachuteTask` is never cancelled after `releaseParachutes()` runs. The task (started at line 71, runs every 2 ticks) continues polling `fallingCrate.isDead()` and returning early indefinitely. It only stops when `cancel()` is called externally or the plugin disables. Each airdrop leaves a lingering repeating task consuming scheduler resources. The comment at line 98-100 acknowledges this but doesn't resolve it — a deferred cancellation (e.g., cancel in the delayed cleanup runnable at line 111-117) would be safe.
- [ ] 27. **HIGH**: `Crate.opened` field uses `Boolean` wrapper type instead of primitive `boolean` (`Crate.java:52`). `setOpened()` at line 226 auto-unboxes with `if (opened)` — if `setOpened(null)` is ever called, this throws `NullPointerException`. Additionally, `opened` is not `volatile` and is read/written from different threads (main thread via listeners, potentially async tasks via `getOpened()`), creating a visibility issue.
- [ ] 28. **HIGH**: `CrateCleanupListener` has dead code — the `isCancelled()` checks at lines 22, 30, 38 are unreachable because the `@EventHandler` annotation uses `ignoreCancelled = true`, which means Bukkit skips the handler entirely for cancelled events. The checks should be removed to avoid misleading future maintainers.
- [ ] 29. **HIGH**: `CrateManager.removeCrate(Location):57-67` calls `crate.destroy()` as a side effect of map removal. This means `CrateDestroyListener` (which calls `removeCrate` at line 21) and `CrateCloseListener` (which calls `removeCrate` at line 39) both trigger full crate destruction including particle task cancellation, but `CrateCloseListener` also calls `barrel.getBlock().setType(Material.AIR)` at line 38 **before** `removeCrate` — so `destroy()` tries to operate on an already-removed block. The coupling between map removal and destruction is fragile and should be explicit.
- [ ] 47. **HIGH**: `PackageGui.onInventoryClick():64` and `CreatePackageGui.onInventoryClick():64` cast `e.getWhoClicked()` to `Player` without `instanceof` check. `HumanEntity` returned by `getWhoClicked()` could theoretically be a non-Player entity. Unlike `PackagesGui:87` which correctly uses `instanceof Player p` pattern matching, these GUIs perform unchecked casts. The cast happens **before** the `e.getInventory().equals(inv)` check, so it fires on every `InventoryClickEvent` for any inventory.
- [ ] 48. **HIGH**: `PackageGui.onInventoryDrag():112` and `CreatePackageGui.onInventoryDrag():106` cast `e.getWhoClicked()` to `Player` without `instanceof` check. Same unchecked cast issue as #47 but in the drag event handlers.
- [ ] 60. **HIGH**: `ParachuteSystem.releaseParachutes()` has no re-entrance guard and is called every 2 ticks from the uncancelled `parachuteTask`. After the falling block dies, every task tick calls `releaseParachutes()`, which each time schedules a NEW delayed cleanup task at line 111. Over a 10-second window before `cancel()` is called, this creates ~100 redundant delayed tasks, each calling `remove()` on already-dead entities. A boolean `released` flag should prevent re-entry.
- [ ] 61. **HIGH**: `Crate.destroy():160-180` does not remove the barrel block when called on a LANDED crate. Only `stopEffects()` is called. When `CrateManager.clearAll()` is called during plugin disable, barrel blocks with their inventory contents remain in the world as orphaned blocks that are no longer tracked. Compare with `CrateCloseListener:38` and `CrateDestroyListener` which do remove the block.
- [ ] 62. **HIGH**: `PackagesGui` never unregisters its event listener on inventory close. Unlike `PackageGui` (line 119-123) and `CreatePackageGui` (line 113-117) which unregister in `onInventoryClose`, `PackagesGui` has no close handler. Since it's a singleton held by `Airdrop.packagesGui`, every `InventoryClickEvent` and `InventoryDragEvent` on the server is processed by `PackagesGui`'s handler even when no player has it open.
- [ ] 63. **HIGH**: `PackageGui` registers a new listener instance on every package click in `PackagesGui.onInventoryClick():104-106`. If a player disconnects without closing the inventory (kick, crash, server stop), the `InventoryCloseEvent` may not fire, leaving orphaned listeners. Each orphaned listener processes every subsequent `InventoryClickEvent`, `InventoryDragEvent`, and `InventoryCloseEvent` on the server.
- [ ] 64. **HIGH**: No input sanitization on package names in `PackageController.createPackageCommand():89`. Package names are taken from user input (`args[2]`) and used directly as YAML configuration keys. Names containing `.`, `:`, or other YAML special characters create nested paths or corrupt `packages.yml`. For example, creating a package named `"test.items"` would create a nested `test` -> `items` path rather than a key named `test.items`.
- [ ] 65. **HIGH**: `Package.chargeUser():70-73` throws `CannotAffordException` when `economy == null`, but the actual problem is a missing economy provider (Vault not installed), not that the player can't afford the package. The error message shown to the player will incorrectly say they cannot afford it. Should throw a distinct exception or log a warning about the missing economy provider.
- [ ] 86. **HIGH**: `RenderPackageLandedTask` is launched with `runTaskAsynchronously()` (`Crate.java:124`) which executes the `run()` method exactly **once**. However, the task has repeating-task logic: `ticksElapsed` counter, `MAX_TICKS = 20`, and `this.cancel()` on completion (`RenderPackageLandedTask.java:33-36`). The entire 20-tick landing animation (rising glow particles + shrinking ground wave) only renders a single frame. Should use `runTaskTimer(plugin, 0L, 1L)` (sync, per issue #38) to actually animate over 20 ticks.
- [ ] 87. **HIGH**: `ParachuteSystem.releaseParachutes():111` schedules a delayed cleanup task via `Bukkit.getServer().getScheduler().runTaskLater(Airdrop.getPluginInstance(), ...)`. During `onDisable()`, `CrateManager.clearAll()` calls `crate.destroy()` → `parachuteSystem.cancel()`, which removes entities immediately. But `releaseParachutes()` may have already been called by the parachute task, scheduling a delayed cleanup 60 ticks later. After `onDisable()` sets `pluginInstance = null` (Airdrop.java:105), the pending delayed task holds a reference to the old plugin instance. If the scheduler processes it after disable, it operates on stale entity references.
- [ ] 88. **HIGH**: `CrateCleanupListener` handles `ChunkUnloadEvent` but not `WorldUnloadEvent`. If an entire world is unloaded (e.g., Multiverse world management), crates in that world remain in both `crateMap` and `landedCrateMap` with stale world references, causing NPE on subsequent chunk-based lookups that call `fallingBlock.getWorld()` or `location.getWorld()`.
- [ ] 89. **HIGH**: `Crate.land():110` declares local variable `BlockState state = blockChest.getState()` which shadows the instance field `this.state` (the `Crate.State` enum). While not currently a bug (the field is already set to `LANDED` at line 106), any future code in the `land()` method that references `state` without `this.` will get the `BlockState` instead of the `Crate.State`, creating a maintenance trap.
- [ ] 104. **HIGH**: `CmdAirdrop.onCommand():27-29` calls `Airdrop.getVersion()` and `Airdrop.getPluginApiVersion()` without null checks. After `onDisable()` nulls these static fields (Airdrop.java:106-107), or before `onEnable()` assigns them, calling `/airdrop version` passes `null` values to `Map.of()` which throws `NullPointerException`. While unlikely during normal operation, it's reachable during plugin reload races.
- [ ] 105. **HIGH**: `CmdAirdrop.onCommand():35-38` accesses `Airdrop.getConfiguration()` and `Airdrop.getPluginInstance()` in the reload handler without null checks. If a concurrent `onDisable()` nulls these between the calls, `NullPointerException` is thrown during reload. The reload path also calls `PackageManager.reload()` which calls `Airdrop.getPackagesConfiguration()` and `Airdrop.getPluginInstance().setupPackageGuis()` — all of which are null-unsafe after disable.
- [ ] 106. **HIGH**: `Airdrop.onDisable():98-111` nulls static references (`pluginInstance`, `configuration`, `packagesConfiguration`) AFTER `CrateManager.clearAll()`, but clearAll triggers `crate.destroy()` which calls `parachuteSystem.cancel()`. If any parachute task is mid-execution during clearAll, it accesses `Airdrop.getPluginInstance()` for scheduling, which is still non-null. However, after clearAll returns, the nulling at lines 105-111 creates a race with any already-scheduled delayed tasks (from `releaseParachutes()`) that haven't fired yet. These tasks will NPE when they try to access stale plugin references. Should cancel all Bukkit tasks for this plugin before nulling references: `Bukkit.getScheduler().cancelTasks(this)`.
- [ ] 107. **HIGH**: `Config.onConfigLoaded():23-27` opens an `InputStreamReader` wrapping `plugin.getResource()` but never closes either stream. This leaks a native file handle from the plugin JAR on every config reload. Same pattern as issue #30 for `LanguageManager`. Unlike `LanguageManager` where the stream is opened once, `Config.onConfigLoaded()` is called on every `reloadConfig()` invocation, compounding the leak.

## Open - Medium

- [ ] 30. `LanguageManager.loadLanguage():47-50` — `InputStreamReader` wrapping `plugin.getResource()` is never closed. The stream should use try-with-resources to prevent resource leaks on each `loadLanguage()` / `reload()` call.
- [ ] 31. `en.yml` is missing keys present in `MessageKey.java`: `packages.price-invalid`, `packages.item-limit`, and `system.package-price-invalid`. While the `LanguageManager` defaults sync mechanism will add these on first load from compiled defaults, the bundled `en.yml` resource is incomplete. If a server admin translates `en.yml`, these messages appear in English regardless because the bundled default also lacks them.
- [ ] 32. `PackageManager.packages` is a plain `HashMap` (line 31) accessed from multiple contexts (main thread commands, GUI events, reload). While Bukkit events are typically on the main thread, a `/airdrop reload` during GUI interaction could cause `ConcurrentModificationException` if `populatePackages()` calls `packages.clear()` while iteration is occurring.
- [ ] 33. `CrateManager` uses both `ConcurrentHashMap` (thread-safe) and `synchronized` methods (lines 32-75), which is redundant. Either remove `synchronized` and rely on `ConcurrentHashMap`'s atomicity, or switch to regular `HashMap` with `synchronized`. The current approach adds unnecessary contention without additional safety.
- [ ] 34. `CreatePackageGui` registers itself as a listener in the constructor (line 44) before `openInventory()` is called. If an exception occurs between construction and opening, the handler stays registered indefinitely. `PackageGui` uses external registration (done at the call site in `PackagesGui`). These should follow a consistent pattern.
- [ ] 35. `DropController.getSpawnLocation():141` compares `loc.getBlockY()` against `highestLocation.getBlockY()` using `<=`, but `loc` is the player's location (fractional Y) while `highestLocation` comes from `getHighestBlockAt()` (integer Y). A player standing exactly at ground level (`blockY == highestBlockY`) is rejected with `SkyNotClearException`, even though the sky above them may be clear. The check should use `<` or compare the actual sky column.
- [ ] 37. `Crate.land():115-116` calls `barrel.getInventory().addItem(is)` but doesn't call `barrel.update()` afterward. Some Paper versions require `BlockState.update()` to persist inventory changes to the world. Without it, items may not appear in the barrel for clients.
- [ ] 41. `PackageManager.updatePackageInventory():207` and `createPackage():234` serialize items using `limitedItems.toArray()` which produces an `Object[]`. Bukkit's YAML serializer should handle `ItemStack` deserialization, but an explicit `new ArrayList<>(limitedItems)` typed list or `limitedItems.toArray(new ItemStack[0])` would be safer to ensure proper round-trip serialization.
- [ ] 43. `PACKAGES_INFO` message key (`MessageKey.java:12-13`) template contains `{info}` placeholder. Callers providing this message (e.g., `PackageCommand`) must ensure the `{info}` placeholder is populated. If the placeholder map doesn't include `info`, the literal string `{info}` will appear in chat.
- [ ] 44. `ParachuteSystem.cancel()` (`ParachuteSystem.java:124-136`) removes entities but never cancels `parachuteTask`. If `cancel()` is called while the task is still running (e.g., during `Crate.destroy()`), the task continues executing and calls `fallingCrate.isDead()` / `fallingCrate.getLocation()` on a potentially removed entity, causing errors. The `parachuteTask` field should be cancelled in `cancel()`: `if (parachuteTask != null && !parachuteTask.isCancelled()) parachuteTask.cancel();`. This is the concrete fix for issue #26.
- [ ] 45. `CrateManager.removeCratesInChunk()` (`CrateManager.java:106-137`) calls `crate.destroy()` for each crate in the chunk, but uses `iterator.remove()` directly — bypassing `removeCrate(Location)` and its destroy() side effect. This is actually correct (avoids double-destroy), but inconsistent with how `CrateCloseListener`/`CrateDestroyListener` remove crates. The two removal paths should be unified behind a single method.
- [ ] 46. `PackagesGui.onInventoryClick():94` lowercases the display name before package lookup: `String packageName = displayName.toLowerCase()`. Package names are stored as-is from config. If a package is named "StarterPack" in `packages.yml`, the GUI lookup with "starterpack" will fail via `PackageManager.get()`, since the backing `HashMap` uses exact string matching. This is the root cause of issue #13 — `PackagesGui` normalizes to lowercase but `PackageManager.get()` does exact match.
- [ ] 49. `PackageManager.has()` (`PackageManager.java:173`) returns `Boolean` wrapper type instead of primitive `boolean`. This is unnecessary boxing — should be `public static boolean has(String packageName)` to avoid potential auto-unboxing NPE if used in boolean context.
- [ ] 50. `AirdropTabCompleter` suggests `[packageName]` as a literal tab-completion option (line 16). This is a placeholder string, not a real suggestion. The tab completer should instead suggest actual package names from `PackageManager.getPackages()` when the player starts typing the first argument.
- [ ] 51. `PackageManager.populatePackages():108` performs an unchecked cast `(ConfigurationSection) config.get(pkg)`. If the config entry under `pkg` is not a `ConfigurationSection` (e.g., a plain string or number due to config corruption), this will throw `ClassCastException` and abort loading all remaining packages. Should use `config.getConfigurationSection(pkg)` instead, which returns null safely.
- [ ] 52. `PackagesGui.packageGuiItem():59-64` catches `PackageNotFoundException` but still creates a GUI item with `price = 0.0` for a package that doesn't exist in the in-memory registry. This creates phantom entries in the packages GUI that will always fail when clicked. Should skip the item entirely or log a warning.
- [ ] 53. `Crate.land():115-116` iterates `contents` list and calls `barrel.getInventory().addItem(is)` but discards the return value. `addItem()` returns a `HashMap<Integer, ItemStack>` of items that couldn't fit. If the barrel is somehow full, items are silently lost with no warning.
- [ ] 55. `ParachuteSystem.releaseParachutes():111-117` schedules chicken cleanup 60 ticks later. If the chunk unloads before the cleanup runs, the chickens persist as orphaned entities in the world save. The delayed cleanup should check `chicken.isValid()` before `remove()` and a more immediate cleanup should be considered.
- [ ] 56. `CreatePackageGui` constructor lowercases the package name at line 35 (`this.name = name.toLowerCase()`), but `PackageGui` does not normalize. This creates an inconsistency where packages created via GUI are always lowercase, but packages created via `PackageManager` directly or from config can be mixed-case. All name normalization should happen in one place (`PackageManager`).
- [ ] 108. `PackageManager.reload():62-67` calls `Airdrop.getPackagesConfiguration().reloadConfig()`, then `populatePackages()`, then `Airdrop.getPluginInstance().setupPackageGuis()`. The `setupPackageGuis()` call unregisters and re-registers the `PackagesGui` listener. If `reload()` is called from within a `PackagesGui` event handler (e.g., indirectly via `PackageGui.save()` → `PackageManager.updatePackageInventory()` → `PackageManager.reload()`), the listener being currently executing gets unregistered mid-handling. Bukkit tolerates this, but the old `PackagesGui` inventory reference that the player has open becomes detached from the new `PackagesGui` instance — further clicks in the still-open inventory are silently ignored.
- [ ] 109. `PackageManager.updatePackageInventory():209` calls `fileConfig.set(PACKAGES, config)` which sets the entire `packages` section to the ConfigurationSection object. This pattern is fragile — `ConfigurationSection.set()` with a ConfigurationSection value copies the data, but if the implementation returns a section backed by the same root config, this can create circular references or lose data from other packages. The safer approach is to only `set()` the specific package key that changed.
- [ ] 110. `PackageManager.deletePackage():252` calls `config.set(packageName, null)` followed by `fileConfig.set(PACKAGES, config)`. The first call removes the key from the section, then the second copies the modified section back. If the section is already the child of `fileConfig`, the second call is redundant but harmless. However, combined with `PackageManager.reload()` at line 255, this triggers a full reload including GUI rebuild, which is heavy for a single deletion. A targeted removal approach would be more efficient.
- [ ] 111. `Airdrop.onEnable():92` calls `PermissionsHelper.initialize()` which registers LuckPerms groups. But if `setupEconomy()` fails at line 67-71, the method returns early at line 70 after calling `disablePlugin()`. Since `disablePlugin()` does not throw, execution continues past the return in the if-block, and `onDisable()` is subsequently called. However, `PermissionsHelper.initialize()` at line 92 is never reached in this case, which is correct. The actual issue is that `PackageManager.reload()` at line 90 calls `setupPackageGuis()` which registers listeners — but if economy setup fails, `onDisable()` will try to unregister `packagesGui` which was set by `setupPackageGuis()`. This is benign but wasteful.
- [ ] 112. `PackagesConfig.onCreateDefaultConfig():26` calls `setConfig(new YamlConfiguration())` then `writeDefaults()` then `saveConfig()`. The `saveConfig()` at `AbstractConfig:64` checks `if (config == null || configFile == null)` — `configFile` is set by `saveDefaultConfig()` during `initializeConfig()`, so it should be non-null. However, `setConfig()` bypasses the normal `reloadConfig()` path which ensures `configFile` is initialized. If the constructor chain is reordered, `configFile` could be null, making `saveConfig()` silently no-op and losing the default starter package data.

## Open - Low

- [ ] 66. `ParachuteSystem` slime entity (`parachuteLeash`) is not set to `setSilent(true)` at line 46-50. Players near the drop will hear slime idle/movement sounds from the invisible leash entity.
- [ ] 67. `ParachuteSystem` chickens are not set to `setAI(false)` at lines 56-59. After release from leashes, they wander with AI for 60 ticks before the cleanup task removes them. During this time they can trigger redstone mechanisms (pressure plates), interact with entities, and cause unintended side effects.
- [ ] 68. Parachute entities (slime and chickens) spawned by `ParachuteSystem` have no scoreboard tag or persistent metadata. Other plugins (mob stacking, protection, etc.) cannot identify them as plugin-managed entities and may interfere with them (e.g., preventing removal).
- [ ] 69. Custom exceptions (`CannotAffordException`, `PackageNotFoundException`, `InsufficientPermissionsException`, `SkyNotClearException`, `DuplicatePackageException`) do not pass a message to `super()`. Calling `exception.getMessage()` returns `null` for all of them, making log output unhelpful if these are ever caught by generic handlers.
- [ ] 70. `Crate.java:197-204` has duplicate Javadoc block — two consecutive `/** Gets the current location... */` comments above the single `getLocation()` method.
- [ ] 72. `CrateCleanupListener.removeCrates():53-58` checks `block.getType() == Material.BARREL` at `MONITOR` priority. At this point the explosion has been processed, so affected blocks may already be converted to AIR. The type check could fail to match airdrop barrels, leaving stale entries in `landedCrateMap` that are never cleaned up. Should check before the explosion processes (lower priority) or use a different identification method.
- [ ] 73. `PackageController.createPackage()` overloads (lines 126-130, 140-144) have incorrect Javadoc `@throws` — documents `@throws PackageNotFoundException` but actually throws `DuplicatePackageException`.
- [ ] 74. Tab completers (`AirdropTabCompleter`, `PackageTabCompletion`) do not filter suggestions by typed prefix. Standard Bukkit convention is to filter the returned list to entries starting with the current argument. Returning all options regardless of input means the client shows irrelevant suggestions while the player types.
- [ ] 75. `CrateDestroyListener.onBlockBreak():21` calls `CrateManager.removeCrate(barrelLocation)` for every barrel broken on the server, acquiring the synchronized lock even for non-airdrop barrels. On servers with frequent barrel interactions, this adds unnecessary contention.
- [ ] 76. `DropCommand.onCommand():40` — when `args.length > 1`, the package is looked up successfully (line 33) but the drop is never executed because the `if (args.length == 1)` check fails. The command silently does nothing with no error message to the player. Extra arguments should either be rejected with a usage message or ignored.
- [ ] 77. `ChatHandler` sends console messages to `Bukkit.getServer().getConsoleSender()` instead of using `sender.sendMessage()` for non-Player senders. Command blocks and RCON clients never receive command responses — the message goes to the console instead.
- [ ] 78. `en.yml` has a dead key `packages.info-price` (line 11) with no corresponding `MessageKey` enum entry. Additionally, `en.yml` line 52 says "Spigot API Version" while the `MessageKey.SYSTEM_VERSION_INFO` default says "Paper API" — mismatch since this project specifically targets Paper.
- [ ] 79. `build.gradle.kts` uses `load = STARTUP` (line 75) which loads the plugin before worlds are initialized. Airdrop is a gameplay plugin that requires worlds for entity spawning — the standard load order is `POSTWORLD`.
- [ ] 80. `build.gradle.kts` lists Vault as a hard `depend` (line 78), but economy is configurable via `economy.enabled`. If a server disables economy, the plugin still fails to load without Vault installed. Vault should be a `softdepend` if economy is optional.
- [ ] 81. CLAUDE.md says "paper-plugin.yml is generated by Gradle (net.minecrell.plugin-yml.paper)" but `build.gradle.kts:4` actually uses `net.minecrell.plugin-yml.bukkit`, which generates `plugin.yml` (Bukkit format). Documentation is out of sync with reality.
- [ ] 82. `Crate.getLocation():202-204` returns the internal `dropLocation` or `landedLocation` without cloning. Callers can mutate the crate's internal state through the returned reference. Same applies to `getDropLocation():210` and `getLandedLocation():217`.
- [ ] 83. `Crate.stopEffects():148-155` does not cancel the `RenderPackageLandedTask`. When called via `setOpened(true)` at line 227, the landed task continues running (up to 20 ticks) even though the crate has been opened. No reference to the landed task is stored for later cancellation.
- [ ] 84. `CrateManager.getCrateMap():154-156` (deprecated) returns the internal `ConcurrentHashMap` directly, allowing external code to bypass all synchronized access methods and mutate the map without synchronization.
- [ ] 85. `Airdrop.setPluginInstance()` and `setLuckPerms()` are public static — any code can override the plugin instance or LuckPerms API reference. These setters should be package-private or removed.
- [ ] 90. `RenderPackageGlowTask` and `RenderPackageSmokeTask` constructors use `location.getBlock().getLocation().add(...)` which safely creates a new location. But `RenderFlareTask:31` uses `location.getBlock().getLocation().add(0.5, 0.0, 0.5)` which also creates a new reference. Only `RenderPackageLandedTask:27` mutates the passed-in location directly. The inconsistency makes the landed task bug (#25) harder to spot during review.
- [ ] 91. `Crate.java:138` plays landing sound at `landedLocation` which by this point has already been mutated by `RenderPackageLandedTask` constructor (issue #25) — the sound plays at (x+0.5, y+0.5, z+0.5) instead of the actual block location. The sound offset is audible to nearby players.
- [ ] 92. `DropController.getSpawnLocation():138` calls `world.getHighestBlockAt()` which returns the highest **non-air** block. In void worlds or over deep caves, this returns the bottom of the world (y=min), placing the spawn location far below the player. No minimum height or sanity check is performed.
- [ ] 93. `Airdrop.setupPackageGuis():126-132` creates a new `PackagesGui` and registers it as a listener, but `PackagesGui` constructor calls `initializeItems()` which reads from `PackageManager`. If `setupPackageGuis()` is called before `PackageManager.reload()` (e.g., during reload ordering), the GUI will be populated with stale or empty package data.
- [ ] 94. `CreatePackageGui` uses default locale `name.toLowerCase()` at line 35, but `PermissionsHelper.hasPermission():77` normalizes with `Locale.ROOT`. In Turkish locale, "I" lowercases to "ı" (dotless i) with default locale but to "i" with `Locale.ROOT`, creating a mismatch between the stored package name and the permission node. All lowercase operations on package names should use `Locale.ROOT` consistently.
- [ ] 95. `PackagesGui.initializeItems():42-46` calls `inv.addItem()` but never calls `inv.clear()` first. If `initializeItems()` were ever called a second time (e.g., during a hypothetical GUI refresh), items would be appended rather than replaced, doubling the entries. Currently it's only called once in the constructor, but the method is public and misleadingly named as an initializer.
- [ ] 96. `PackagesCommand.onCommand():27` calls `Airdrop.getPackagesGui().openInventory(player)` without null-checking `getPackagesGui()`. If `setupPackageGuis()` hasn't been called yet (e.g., during early startup race condition or after a failed reload), this throws `NullPointerException`.
- [ ] 97. `AirdropTabCompleter.onTabComplete():28` creates a new `PackageTabCompletion()` instance on every tab completion event for the "package" subcommand. This is wasteful — the tab completer should be created once and reused.
- [ ] 98. `Crate.land():106` sets `this.state = State.LANDED` before validating that the barrel block state creation succeeds (lines 109-113). If `blockChest.getState()` doesn't return a `Barrel` instance, the `IllegalStateException` is thrown but the crate is permanently stuck in `LANDED` state with no barrel, no inventory, and no way to recover. The state transition should happen after validation succeeds.
- [ ] 99. `PackageManager.getPackages():74-77` reads keys from the file-based `ConfigurationSection` (via `getPackagesSection()`), but `get():86-93` looks up from the in-memory `packages` HashMap. After a config file edit without reload, these two sources diverge — `getPackages()` returns keys from disk while `get()` uses stale memory. This also causes `PackagesGui.initializeItems()` to show packages that don't exist in memory, leading to `PackageNotFoundException` on click.
- [ ] 100. `Crate.land():119` stores the barrel location via `barrel.getLocation()` which returns a `BlockState` location, while `landedLocation` at line 105 is set to `block.getLocation().clone()`. These could theoretically differ. The key stored in `CrateManager` (line 119) should use the same `landedLocation` field for consistency: `CrateManager.addCrate(this.landedLocation, this)`.
- [ ] 101. `FallingCrateListener.onEntityChangeBlockEvent():33` cancels the event (`e.setCancelled(true)`) to prevent the falling block from becoming a natural barrel. However, cancelling `EntityChangeBlockEvent` at `NORMAL` priority means other plugins listening at lower priority may see the event as cancelled but not know why. The handler should use `EventPriority.HIGH` to cancel before other plugins process at `NORMAL`.
- [ ] 102. `PackageManager.getFileConfig():36-38` calls `Airdrop.getPackagesConfiguration().getConfig()` without null-checking `getPackagesConfiguration()`. After `onDisable()` sets `packagesConfiguration = null`, any late call (e.g., from a delayed task or async operation) throws `NullPointerException`. Same pattern as issue #57 but for the packages config.
- [ ] 113. `PackageDropEvent` and `PackageLandEvent` expose mutable `Location` references directly from the crate via constructor parameters. Listeners to these events can mutate the location objects, indirectly corrupting the crate's internal state. Event locations should be cloned.
- [ ] 114. `PackageLandEvent` is fired at `FallingCrateListener:37-38` with `loc` (the entity's location) and `loc.getBlock()` which is the block at the entity's feet position. But `Crate.land()` is called at line 36 with `loc.getBlock()`, which sets the barrel at the block position. The `landingLocation` in the event is the raw entity location (with fractional coordinates), not the barrel block location. Listeners expecting the barrel location will get wrong coordinates.
- [ ] 115. `Gui.getControlItemNames():14-18` creates a new `Arrays.asList()` on every call by invoking `ChatHandler.get()` three times. This is called from `isControlItem()` which is called from `PackageManager.sanitizePackageItems()` for every `ItemStack` in a package during save/load. For a package with many items, this creates redundant allocations. The control names should be cached or computed once.
- [ ] 116. `Package.setItems():175-181` replaces the `items` field reference but does not create a defensive copy. The caller retains a reference to the same list, so mutations outside of `Package` can corrupt the internal state. For example, `PackageManager.populatePackages():134` passes `limitedItems` which is then stored directly.
- [ ] 117. `Crate` constructor stores `world` as a field (`Crate.java:64`) but `dropLocation` already contains a world reference. If the `world` parameter differs from `location.getWorld()`, there's an inconsistency. The world should be derived from the location, or the location's world should be validated against the parameter.
- [ ] 118. `RenderFlareTask.java:83-86` has a `stopEffect()` method that sets `shouldContinue = false` and then calls `cancel()`. The `cancel()` alone is sufficient since `run()` checks `shouldContinue` at line 37. If `stopEffect()` is called from outside the task's thread, there's a brief window where `shouldContinue` is set but the task hasn't checked it yet and `cancel()` is the real termination. The `shouldContinue` flag is effectively redundant with `cancel()`.
- [ ] 119. `PackageTabCompletion:36` returns `List.of("[packageName]")` as a tab completion hint for both `create` and `delete` subcommands. For `delete`, it would be more helpful to suggest existing package names (from `PackageManager.getPackages()`) since the user needs to type an existing package name.
