<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.airdropmc</groupId>
    <artifactId>airdrop-lightkeeper-tests</artifactId>
    <version>4.0.0-SNAPSHOT</version>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lightkeeper.version>be585af08221c37bcbc8c9d7f5a40a27dbd2dff1</lightkeeper.version>
        <lightkeeper.plugin-adapter.version>be585af08221c37bcbc8c9d7f5a40a27dbd2dff1-airdrop1</lightkeeper.plugin-adapter.version>
        <paper.version>1.21.11-R0.1-SNAPSHOT</paper.version>
        <junit.version>6.1.3</junit.version>
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <maven.failsafe.version>3.5.6</maven.failsafe.version>
        <lightkeeper.runtime-manifest>${project.build.directory}/lightkeeper/runtime-manifest.json</lightkeeper.runtime-manifest>
        <lightkeeper.server-work-directory>${project.build.directory}/lightkeeper-server</lightkeeper.server-work-directory>
    </properties>

    <repositories>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>lightkeeper-plugin-adapter</id>
            <url>file://${project.basedir}/target/lightkeeper-plugin-repository</url>
        </pluginRepository>
        <pluginRepository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </pluginRepository>
    </pluginRepositories>

    <dependencies>
        <dependency>
            <groupId>com.github.PimvanderLoos.LightKeeper</groupId>
            <artifactId>lightkeeper-framework-junit</artifactId>
            <version>${lightkeeper.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paper.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.version}</version>
            </plugin>

            <plugin>
                <groupId>com.airdropmc.lightkeeper-adapter</groupId>
                <artifactId>lightkeeper-maven-plugin</artifactId>
                <version>${lightkeeper.plugin-adapter.version}</version>
                <configuration>
                    <userAgent>Airdrop/4.0 (https://github.com/LukeMccon/Airdrop)</userAgent>
                    <configOverlayPath>${project.basedir}/src/test/resources/overlay</configOverlayPath>
                    <plugins>
                        <plugin>
                            <sourceType>path</sourceType>
                            <path>${airdrop.jar.path}</path>
                            <renameTo>Airdrop.jar</renameTo>
                        </plugin>
                        <plugin>
                            <sourceType>modrinth</sourceType>
                            <modrinthVersionId>b0mk8uS6</modrinthVersionId>
                            <modrinthLoader>bukkit</modrinthLoader>
                            <modrinthGameVersion>1.21.11</modrinthGameVersion>
                            <renameTo>LuckPerms.jar</renameTo>
                        </plugin>
                    </plugins>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-paper</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>prepare-server</goal>
                        </goals>
                        <configuration>
                            <serverType>paper</serverType>
                            <serverVersion>1.21.11</serverVersion>
                            <runtimeManifestPath>${lightkeeper.runtime-manifest}</runtimeManifestPath>
                            <serverWorkDirectoryRoot>${lightkeeper.server-work-directory}</serverWorkDirectoryRoot>
                        </configuration>
                    </execution>
                    <execution>
                        <id>cleanup-paper</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>cleanup-server</goal>
                        </goals>
                        <configuration>
                            <deleteTargetServerOnSuccess>true</deleteTargetServerOnSuccess>
                            <failsafeSummaryPath>${project.build.directory}/failsafe-reports/failsafe-summary.xml</failsafeSummaryPath>
                            <serverWorkDirectoryRoot>${lightkeeper.server-work-directory}</serverWorkDirectoryRoot>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${maven.failsafe.version}</version>
                <configuration>
                    <includes>
                        <include>**/*IT.java</include>
                    </includes>
                    <reportsDirectory>${project.build.directory}/failsafe-reports</reportsDirectory>
                    <summaryFile>${project.build.directory}/failsafe-reports/failsafe-summary.xml</summaryFile>
                    <systemPropertyVariables>
                        <lightkeeper.runtimeManifestPath>${lightkeeper.runtime-manifest}</lightkeeper.runtimeManifestPath>
                        <lightkeeper.diagnosticsDirectory>${project.build.directory}/lightkeeper-reports</lightkeeper.diagnosticsDirectory>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
